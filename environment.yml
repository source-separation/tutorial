name: tutorial-environment
channels:
  - conda-forge
  - defaults
dependencies:
  - libsndfile
  - ffmpeg
  - pip:
    - jupyter-book
    - jupyterlab
    - numpy
    - matplotlib
    - librosa
    - nussl
    - scaper
    - soxbindings